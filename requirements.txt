# Development environment for wanderwalk.
#
# Runtime dependencies are declared in pyproject.toml, which is the single
# source of truth. This file just installs the project in editable mode with
# every optional extra, for contributors who prefer:
#
#     pip install -r requirements.txt
#
# Library users should instead run one of:
#
#     pip install wanderwalk           # core library, numpy only
#     pip install wanderwalk[app]      # plus the Streamlit app

-e .[app,notebooks,test]
